<view class="content">
    <view class="screen">
    {{screenData}}
    </view>
    <view class="btnGap">
        <view class="item orange" bindtap="exitGrid" id="{{id1}}">
         <!--<icon class="icons" type="download" size="25" color="#999"/>-->
        退格  
        </view>
        <view class="item orange" bindtap="exitGrid" id="{{id2}}">
             AC
        </view>
        <view class="item orange" bindtap="exitGrid" id="{{id3}}">
             +/-
         </view>
         <view class="item orange" bindtap="exitGrid" id="{{id4}}">
              +
          </view>
    </view>

    <view class="btnGap">
        <view class="item blure" bindtap="exitGrid" id="{{id5}}">
           9
        </view>
        <view class="item blure" bindtap="exitGrid" id="{{id6}}">
             8
        </view>
        <view class="item blure" bindtap="exitGrid" id="{{id7}}">
            7
         </view>
         <view class="item orange" bindtap="exitGrid" id="{{id8}}">
             -
          </view>
    </view>

    <view class="btnGap">
        <view class="item blure" bindtap="exitGrid" id="{{id9}}">
           6
        </view>
        <view class="item blure" bindtap="exitGrid" id="{{id10}}">
             5
        </view>
        <view class="item blure" bindtap="exitGrid" id="{{id11}}">
             4
         </view>
         <view class="item orange" bindtap="exitGrid" id="{{id12}}">
           x
          </view>
    </view>

    <view class="btnGap">
        <view class="item blure" bindtap="exitGrid" id="{{id13}}">
           3
        </view>
        <view class="item blure" bindtap="exitGrid" id="{{id14}}">
             2
        </view>
        <view class="item blure" bindtap="exitGrid" id="{{id15}}">
             1
         </view>
         <view class="item orange" bindtap="exitGrid" id="{{id16}}">
          %
          </view>
    </view>

    <view class="btnGap">
        <view class="item blure" bindtap="exitGrid" id="{{id17}}">
           0
        </view>
        <view class="item blure" bindtap="exitGrid" id="{{id18}}">
           .
        </view>
        <view class="item blure" bindtap="history" id="{{id19}}">
             历史
         </view>
         <view class="item orange" bindtap="exitGrid" id="{{id20}}">
          =
          </view>
    </view>
</view>
